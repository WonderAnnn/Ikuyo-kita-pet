# 默认皮肤资源目录

正式默认皮肤必须是原创或获得明确再分发授权的透明 PNG/WebP 素材。

最小资源集：

```text
manifest.json
idle.png
remind.png
```

仓库骨架暂不放入人物图片，避免把未确认许可的商业动漫角色素材加入开源历史。后续可使用 Codex `imagegen` 生成原创透明桌宠，再按 `schemas/skin-manifest.schema.json` 校验。
