# 本轮性能与诊断优化执行记录

日期：2026-09-14

本轮按“子代理实现 → 主线程验证 → 质量代理复核”执行，未提交或清理用户已有工作树改动。

## 已完成

- 任务 1：新增 `scripts/measure-runtime.ps1` 与脚本契约测试；输出启动时间、CPU、私有工作集和样本数，不记录窗口标题。
- 任务 2：新增 `BubbleAssetCache`，统一缓存 source/filled 位图；支持失败缓存、并发单次加载、清理和快照；质量审查确认返回值不可变。
- 任务 3：超长气泡文本改为受限 `ScrollViewer`，按工作区动态设置最大高度，保持原文可读且不溢出；矩阵覆盖三主题、四文本量和三 DPI。
- 任务 4：增加自适应轮询策略和共享前台探针缓存；提醒抑制使用强制刷新，避免缓存过期导致额外延迟；诊断页不可见时不读数据库。
- 任务 5：进程列表增加 10 秒缓存和用户强制刷新，保留无参旧 API；缓存返回只读集合，避免调用方篡改；测量脚本契约同步通过。
- 任务 6：新增 `RuntimeHealthRegistry`、循环/提醒健康快照模型并接入实际 App 装配；记录 `reminder` 与 `work-tracking` 的开始、成功、失败、下一次唤醒和最近提醒元数据，错误只保存稳定错误码。

## 验证证据

- Core 聚焦测试：运行健康注册表 3/3 通过。
- Infrastructure 聚焦测试：提醒循环、工作追踪循环、提醒操作协调器 22/22 通过；进程缓存与运行时测量脚本 6/6 通过。
- 全量 Release 测试：404/404 通过，0 失败。
- Release 构建：0 警告、0 错误。

## 后续待办

任务 7–14 仍是后续提纲：应用环境/资源哈希/数据库健康探针、完整诊断页与脱敏导出、本地滚动日志、提醒调度睡眠与时间变化耐久测试、发布门禁、无障碍和 latest 冒烟文档。它们尚未在本轮冒充完成，执行时继续沿用同一“先测后改、逐项审查、全量回归”流程。
